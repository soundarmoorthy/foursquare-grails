package search.directory.implementations.foursquare

class FourSquareResults {
    //For now just use the json. Converting it to object can be done separately
    String json;

    FourSquareResults(json)
    {
        this.json = json;
    }
}
